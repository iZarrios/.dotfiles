theme = Terafox
font-family = JetBrainsMonoNerdFont
font-size = 10
font-feature = -calt
font-feature = -liga
font-feature = -dlig
gtk-titlebar = false

shell-integration = zsh
shell-integration-features = no-cursor
cursor-style = block
cursor-style-blink = false
title = "Ghostty"

background-opacity = 1
# don't make a confirmation pop-up when I try close the terminal
confirm-close-surface = false

# trying this out
bold-is-bright = true

# even though it is said that it is not supported on Linux but I still want to explicitly disable it
auto-update = off

# don't show resize overlays pop-ups
resize-overlay = never

window-decoration = server
window-subtitle = working-directory
window-theme = dark

# https://ghostty.org/docs/config/reference#window-inherit-working-directory
window-inherit-working-directory = false
# https://ghostty.org/docs/config/reference#working-directory
working-directory = home

app-notifications = no-clipboard-copy

copy-on-select = false

custom-shader-animation = false
